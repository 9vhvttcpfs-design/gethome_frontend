from pathlib import Path\nimport re\nlines = Path('src/App.jsx').read_text(encoding='utf-8').splitlines()\nopen_sum = 0\nclose_sum = 0\nmax_diff = 0\nfor i, line in enumerate(lines, start=1):\n    open_sum += len(re.findall(r'<div\\b', line))\n    close_sum += len(re.findall(r'</div>', line))\n    diff = open_sum - close_sum\n    if diff > max_diff:\n        max_diff = diff\n        print('max diff', max_diff, 'at', i, line.strip())\nprint('final diff', open_sum - close_sum, 'open', open_sum, 'close', close_sum)
